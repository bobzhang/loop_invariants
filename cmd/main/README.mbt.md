# Command Line Entry

This package provides the executable entry point for the module. It is mainly
used for running demos or small checks.

## Usage (conceptual)

```mbt nocheck
moon run cmd/main
```

## Notes

- The main logic lives in the library packages under `lib/`.
